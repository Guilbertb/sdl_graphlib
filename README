sudo apt-get install libsdl1.2-dev
sudo apt-get install libsdl-ttf2.0-dev


pour installer sdl

A propos du Makefile

Ici le projet s'appel test , mais si vous modifiez votre nom de projet 
n'oubliez pas de modifier ce nom aussi dans le Makefile

